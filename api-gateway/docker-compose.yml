version: "3.9"

services:
  app:
    build: .
    container_name: api-gateway-service-v2
    ports:
      - "8095:8080"
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - microservices-net-v2

networks:
  microservices-net-v2:
    external: true